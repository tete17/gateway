global:
  images:
    envoyGateway:
      image: "docker.io/envoyproxy/gateway-dev:latest"
      pullPolicy: Always
certgen:
  certManager:
    enabled: true
    issuerRef:
      name: my-company-ca
      kind: ClusterIssuer
      group: cert-manager.io
